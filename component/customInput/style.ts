import { makeStyles } from "tss-react/mui";
export const useStyles = makeStyles({
  name: "ComponentInput",
  uniqId: "inputComponent",
})(() => ({
  inputStyle: {
    width: "100%",
    height: "44px",
    padding: "20px",
    borderRadius: "70px",
    border: "1px solid #000000",
    fontSize: "14px"
  },
}));
export const style = {};
